name: Publish npm

on:
  push:
    tags:
      - "v*.*.*"
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish, for example v0.1.0. Leave empty to publish the selected ref."
        required: false
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Install dependencies
        run: npm ci

      - name: Verify package
        run: |
          npm run ci
          npm run demo:build
          npm pack --dry-run

      - name: Validate npm publishing access
        run: |
          PACKAGE_NAME="$(node -p "require('./package.json').name")"
          NPM_USER="$(npm whoami)"

          echo "Authenticated to npm as ${NPM_USER}."

          if [[ "${PACKAGE_NAME}" == @*/* ]]; then
            NPM_SCOPE="${PACKAGE_NAME%%/*}"
            NPM_SCOPE="${NPM_SCOPE#@}"

            if ! npm org ls "${NPM_SCOPE}" >/dev/null 2>&1; then
              echo "::error title=Missing npm scope::npm scope @${NPM_SCOPE} does not exist as an npm organization, or this token cannot access it. Create the npm organization '${NPM_SCOPE}' on npmjs.com and add this token as an owner/publisher before publishing ${PACKAGE_NAME}."
              exit 1
            fi

            echo "npm scope @${NPM_SCOPE} exists and is accessible."
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish to npmjs.com
        run: |
          PACKAGE_NAME="$(node -p "require('./package.json').name")"
          PACKAGE_VERSION="$(node -p "require('./package.json').version")"

          if npm view "${PACKAGE_NAME}@${PACKAGE_VERSION}" version >/dev/null 2>&1; then
            echo "${PACKAGE_NAME}@${PACKAGE_VERSION} is already published. Skipping."
            exit 0
          fi

          npm publish --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
